  \documentclass{report}

% Language setting
% Replace `english' with e.g. `spanish' to change the document language
\usepackage[utf8]{inputenc}
\usepackage[polish]{babel}
\usepackage[T1]{fontenc}

\usepackage{array}
\usepackage{booktabs}

% Set page size and margins
% Replace `letterpaper' with `a4paper' for UK/EU standard size
\usepackage[letterpaper,top=2cm,bottom=2cm,left=3cm,right=3cm,marginparwidth=1.75cm]{geometry}

% Useful packages
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage[colorlinks=true, allcolors=black]{hyperref}

\usepackage{float}


\usepackage{listings}
\lstset{
  inputencoding=utf8,
  extendedchars=true,
  literate=
    {ą}{{\k{a}}}1
    {ć}{{\'c}}1
    {ę}{{\k{e}}}1
    {ł}{{\l{}}}1
    {ń}{{\'n}}1
    {ó}{{\'o}}1
    {ś}{{\'s}}1
    {ź}{{\'z}}1
    {ż}{{\.z}}1
    {Ą}{{\k{A}}}1
    {Ć}{{\'C}}1
    {Ę}{{\k{E}}}1
    {Ł}{{\L{}}}1
    {Ń}{{\'N}}1
    {Ó}{{\'O}}1
    {Ś}{{\'S}}1
    {Ź}{{\'Z}}1
    {Ż}{{\.Z}}1,
}

\usepackage{xcolor}

\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

\lstdefinestyle{mystyle}{
    backgroundcolor=\color{backcolour},   
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\footnotesize,
    breakatwhitespace=false,         
    breaklines=true,                 
    captionpos=b,                    
    keepspaces=true,                 
    numbers=left,                    
    numbersep=5pt,                  
    showspaces=false,                
    showstringspaces=false,
    showtabs=false,                  
    tabsize=2
}

\lstset{style=mystyle}

\usepackage{tikz}
\usetikzlibrary{trees}
\usepackage{forest}
\usetikzlibrary{arrows.meta, shapes.geometric, positioning}

\usepackage{authblk}

\usepackage{enumitem}
\usepackage{float}
\usepackage{subcaption}

\title{\LARGE{Klasyfikacja}

\vspace{0.3cm}

\Large{Metody Analizy Danych - zadanie projektowe 2}}

\author{
Joanna Dagil (231008),
Liliana Grześkiewicz (236138),
Piotr Szpatuśko (223420),
Adam Włodarski (230983)
}

\date{rok akademicki 2025/2026}

\begin{document}

\maketitle

\tableofcontents

\chapter*{Streszczenie}
\addcontentsline{toc}{chapter}{Streszczenie}

Celem projektu było porównanie metod klasyfikacji wieloklasowej w zadaniu rozpoznawania siedmiu odmian fasoli na podstawie cech geometrycznych i morfologicznych ziaren. Analizę przeprowadzono na zbiorze \textit{Dry Bean Dataset}. Z dostępnych zmiennych wybrano 12 cech opisujących rozmiar, kształt i proporcje ziaren. Przygotowanie danych obejmowało analizę rozkładów i korelacji, transformację logarytmiczną zmiennych o silnej asymetrii, identyfikację obserwacji odstających za pomocą reguły IQR i odległości Mahalanobisa oraz standaryzację predyktorów.

W badaniu zastosowano wielomianową regresję logistyczną, las losowy, sieć neuronową typu MLP oraz metodę hybrydową opartą na głosowaniu większościowym trzech klasyfikatorów. Jako sposób walidacji wykorzystano stratyfikowany podział typu \textit{hold-out}, w którym 70\% obserwacji przeznaczono do uczenia, a 30\% do testowania. Jakość modeli oceniono na podstawie macierzy pomyłek, globalnej dokładności klasyfikacji oraz wartości \textit{Precision}, \textit{Recall} i \textit{F1-score} wyznaczonych osobno dla każdej klasy.

Wszystkie modele osiągnęły wysoką skuteczność klasyfikacji. Najwyższą wartość \textit{Accuracy}, równą 95{,}46\%, uzyskała metoda hybrydowa. Wielomianowa regresja logistyczna osiągnęła 95{,}41\%, sieć neuronowa 95{,}25\%, a las losowy 94{,}21\%. Dodatkowo przedstawiono przykład użycia wytrenowanych modeli do klasyfikacji sztucznie wygenerowanych obserwacji. Wyniki wskazują, że cechy geometryczne ziaren umożliwiają skuteczne rozróżnianie analizowanych odmian, a połączenie predykcji kilku modeli może ograniczać wpływ pojedynczych błędów klasyfikacji.

\noindent Repo:

\url{https://github.com/joannadagil/DataAnalysisMethods-Classification.git}

\noindent Dane:

\url{https://archive.ics.uci.edu/dataset/602/dry+bean+dataset}



\vspace{0.5cm}
\noindent\textbf{Słowa kluczowe:} klasyfikacja wieloklasowa, wielomianowa regresja logistyczna, \textit{Random Forest}, sieć neuronowa, metoda hybrydowa


% ----------------------------------------------------

\chapter{Wstęp}

Klasyfikacja jest jednym z podstawowych zadań analizy danych i uczenia maszynowego. Polega na przypisywaniu obserwacji do jednej z wcześniej określonych kategorii na podstawie zestawu opisujących je cech. W przypadku klasyfikacji wieloklasowej liczba możliwych kategorii jest większa niż dwie, co wymaga jednoczesnego rozróżniania wielu klas oraz uwzględnienia różnych rodzajów pomyłek popełnianych przez model.

Metody klasyfikacyjne znajdują zastosowanie między innymi w medycynie, przemyśle, finansach oraz rolnictwie. W ostatnim z wymienionych obszarów mogą wspomagać automatyczną ocenę i sortowanie produktów na podstawie informacji pozyskiwanych ze zdjęć. Zastąpienie ręcznej oceny obiektów analizą komputerową pozwala zwiększyć powtarzalność procesu, ograniczyć jego czasochłonność oraz zmniejszyć wpływ subiektywnej oceny człowieka. Przykładem takiego zagadnienia jest rozpoznawanie odmian fasoli na podstawie wymiarów oraz parametrów opisujących kształt ziaren.

W projekcie wykorzystano zbiór \textit{Dry Bean Dataset}, utworzony na podstawie obrazów ziaren siedmiu odmian fasoli: \textit{BARBUNYA}, \textit{BOMBAY}, \textit{CALI}, \textit{DERMASON}, \textit{HOROZ}, \textit{SEKER} oraz \textit{SIRA} \cite{koklu2020}. Każda obserwacja jest opisana zmiennymi geometrycznymi i morfologicznymi, takimi jak pole powierzchni, obwód, długości osi, zwartość, okrągłość czy mimośród. Zadanie polega na wyznaczeniu reguły, która na podstawie tych cech przypisuje pojedyncze ziarno do odpowiedniej odmiany.

Głównym celem analizy jest porównanie modeli różniących się założeniami i sposobem wyznaczania granic decyzyjnych. Zastosowano wielomianową regresję logistyczną jako klasyczny model probabilistyczny, las losowy reprezentujący metody zespołowe oraz wielowarstwową sieć neuronową umożliwiającą modelowanie zależności nieliniowych. Predykcje tych trzech klasyfikatorów połączono następnie w metodzie hybrydowej wykorzystującej głosowanie większościowe.

Istotnym elementem projektu było również odpowiednie przygotowanie danych. Obejmowało ono wybór 12 cech, analizę ich rozkładów i zależności, transformację zmiennych o silnej asymetrii, usunięcie obserwacji odstających oraz standaryzację. Modele oceniono na wspólnym zbiorze testowym z wykorzystaniem mierników dostosowanych do klasyfikacji wieloklasowej. Oprócz globalnej dokładności przeanalizowano macierze pomyłek oraz wartości \textit{Precision}, \textit{Recall} i \textit{F1-score} dla poszczególnych odmian.

W dalszej części raportu przedstawiono charakterystykę zbioru i sposób przygotowania danych, podstawy teoretyczne zastosowanych metod, procedurę walidacji oraz porównanie uzyskanych wyników. Ostatnia część zawiera przykład wykorzystania wytrenowanych modeli do klasyfikacji sztucznie utworzonych obserwacji.

% ----------------------------------------------------


\chapter{Przedmiot badania}

\section{Cel i zakres badania}

Głównym celem niniejszego badania jest wielowymiarowa analiza porównawcza oraz klasyfikacja siedmiu różnych odmian fasoli (\textit{BARBUNYA}, \textit{BOMBAY}, \textit{CALI}, \textit{DERMASON}, \textit{HOROZ}, \textit{SEKER} oraz \textit{SIRA}) na podstawie ich cech geometrycznych i morfologicznych. Badanie ma na celu sprawdzenie, w jakim stopniu parametry kształtu oraz wymiary liniowe wyekstrahowane za pomocą technik przetwarzania obrazu pozwalają na możliwie dokładne przypisanie ziaren do odpowiednich klas botanicznych, co mogłoby zautomatyzować i zobiektywizować proces kontroli jakości w przemyśle spożywczym.

Zakres badania obejmuje:
\begin{itemize}
    \item Przygotowanie danych, w tym analizę struktur korelacji, identyfikację obserwacji odstających oraz transformację zmiennych w celu spełnienia założeń klasycznych modeli statystycznych.
    \item Wybór optymalnego podzbioru 12 cech geometrycznych, charakteryzujących zarówno wielkość, jak i unikalny zarys ziaren.
    \item Budowę wielomianowej regresji logistycznej jako klasycznego modelu probabilistycznego dla klasyfikacji wieloklasowej.
    \item Implementację metod uczenia maszynowego, obejmujących las losowy oraz sieć neuronową, a następnie połączenie ich wyników z regresją logistyczną w modelu hybrydowym.
    \item Ewaluację i porównanie jakości predykcyjnej modeli na podstawie wieloklasowych miar dopasowania, ze szczególnym uwzględnieniem dokładności (\textit{Accuracy}).
\end{itemize}

\section{Wstępna analiza danych}

Wstępna analiza danych stanowi kluczowy etap procesu modelowania, pozwalający na zrozumienie struktury zbioru, rozkładu poszczególnych cech oraz powiązań pomiędzy nimi. Zbiór danych \textit{Dry Bean Dataset} cechuje się wysoką jakością pomiarów, jednak z uwagi na fizyczną naturę obiektów (wymiary ziaren), zmienne charakteryzują się zróżnicowanymi rzędami wielkości oraz silnymi współzależnościami liniowymi.

\subsection{Zmienne wybrane do analizy}

Zgodnie z wymaganiami projektowymi, z oryginalnego zestawu danych wyselekcjonowano 12 ciągłych zmiennych numerycznych. Zostały one podzielone na dwie grupy: zmienne gabarytowe (opisujące bezwzględny rozmiar) oraz bezwymiarowe współczynniki kształtu (opisujące geometrię i proporcje ziaren). Szczegółowe zestawienie oraz interpretację fizyczną wybranych cech przedstawiono w tabeli 2.1.

\begin{table}[H]
\centering
\caption{Zestawienie i opis zmiennych geometrycznych wybranych do analizy}
\label{tab:zmienne_opis}
\begin{tabular}{lll}
\toprule
\textbf{Nazwa zmiennej} & \textbf{Kategoria cechy} & \textbf{Opis matematyczny / fizyczny} \\
\midrule
\textit{Area} & Gabarytowa (Rozmiar) & Pole powierzchni ziarna w pikselach \\
\textit{Perimeter} & Gabarytowa (Rozmiar) & Całkowity obwód zewnętrznej granicy \\
\textit{MajorAxisLength} & Gabarytowa (Rozmiar) & Długość osi głównej elipsy równoważnej \\
\textit{MinorAxisLength} & Gabarytowa (Rozmiar) & Długość osi małej elipsy równoważnej \\
\textit{ConvexArea} & Gabarytowa (Rozmiar) & Pole powierzchni najmniejszego wielokąta wypukłego \\
\textit{EquivDiameter} & Gabarytowa (Rozmiar) & Średnica koła o polu równym polu ziarna \\
\midrule
\textit{Eccentricity} & Kształt (Proporcje) & Mimośród elipsy (miara wydłużenia obiektu) \\
\textit{Roundness} & Kształt (Proporcje) & Współczynnik zaokrąglenia i gładkości brzegu \\
\textit{Compactness} & Kształt (Proporcje) & Zwartość (\textit{EquivDiameter} / \textit{MajorAxisLength}) \\
\textit{Solidity} & Kształt (Proporcje) & Solidność (stosunek \textit{Area} do \textit{ConvexArea}) \\
\textit{AspectRatio} & Kształt (Proporcje) & Współczynnik proporcji osi głównej do małej \\
\textit{Extent} & Kształt (Proporcje) & Stosunek pola powierzchni do prostokąta otaczającego \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Statystyki opisowe i podstawowa wizualizacja}

W celu ilościowego opisu wybranych zmiennych wyznaczono podstawowe charakterystyki rozkładu empirycznego dla całego zbioru danych. Wyniki zestawiono w tabeli 2.2.

\begin{table}[H]
\centering
\caption{Statystyki opisowe wybranych zmiennych numerycznych dla zbioru Dry Bean}
\label{tab:statystyki_opisowe}
\begin{tabular}{lcccccc}
\toprule
\textbf{Zmienna} & \textbf{Średnia} & \textbf{Mediana} & \textbf{Min.} & \textbf{Maks.} & \textbf{Odch. std.} & \textbf{Skośność} \\
\midrule
\textit{Area} & 53048.2 & 44652.0 & 20420.0 & 254616.0 & 29324.1 & 2.95 \\
\textit{Perimeter} & 855.2 & 794.9 & 524.7 & 1985.4 & 214.3 & 1.63 \\
\textit{MajorAxisLength} & 320.1 & 296.8 & 183.6 & 738.9 & 85.7 & 1.36 \\
\textit{MinorAxisLength} & 202.3 & 192.3 & 122.5 & 460.2 & 45.0 & 2.23 \\
\textit{ConvexArea} & 53768.2 & 45170.0 & 20684.0 & 263261.0 & 29774.9 & 2.94 \\
\textit{EquivDiameter} & 253.1 & 238.5 & 161.3 & 569.4 & 59.2 & 1.87 \\
\textit{Eccentricity} & 0.751 & 0.764 & 0.219 & 0.912 & 0.092 & -1.06 \\
\textit{Roundness} & 0.873 & 0.883 & 0.489 & 0.990 & 0.060 & -1.41 \\
\textit{Compactness} & 0.800 & 0.801 & 0.641 & 0.987 & 0.062 & -0.04 \\
\textit{Solidity} & 0.987 & 0.988 & 0.911 & 0.994 & 0.005 & -2.57 \\
\textit{AspectRatio} & 1.583 & 1.551 & 1.024 & 2.430 & 0.247 & 0.78 \\
\textit{Extent} & 0.750 & 0.753 & 0.555 & 0.866 & 0.049 & -0.90 \\
\bottomrule
\end{tabular}
\end{table}

Analiza statystyk opisowych wskazuje na występowanie silnej asymetrii prawostronnej w przypadku zmiennych gabarytowych (np. \textit{Area}, \textit{ConvexArea}), co wyraźnie obrazuje dodatnia, wysoka wartość współczynnika skośności. Wynika to bezpośrednio z obecności odmiany \textit{BOMBAY}, której ziarna są wielokrotnie większe od pozostałych gatunków. Wizualizacja rozkładów za pomocą wykresów pudełkowych (ang. \textit{boxplots}) oraz histogramów potwierdza, że odmiany różnią się drastycznie rozrzutem cech rozmiarowych, podczas gdy cechy kształtu (np. \textit{Solidity}, \textit{Roundness}) wykazują rozkłady bardziej skupione wokół swoich wartości centralnych, wykazując najczęściej asymetrię lewostronną.

\subsection{Transformacje danych}

Z uwagi na fakt, że cechy gabarytowe przyjmują wartości rzędu dziesiątek tysięcy, a bezwymiarowe współczynniki kształtu mieszczą się w wąskim przedziale $(0,1)$, bezpośrednie zastosowanie metod opartych na odległościach lub analizie kowariancji mogłoby prowadzić do dominacji zmiennych o największej skali wartości. W celu ujednolicenia wpływu wszystkich cech na proces klasyfikacji przeprowadzono preprocessing danych obejmujący transformację logarytmiczną oraz standaryzację.

W pierwszym etapie przeanalizowano asymetrię rozkładów wszystkich wybranych zmiennych numerycznych za pomocą współczynnika skośności. Zmienne charakteryzujące się wysoką asymetrią prawostronną, w szczególności zmienne gabarytowe takie jak \texttt{Area}, \texttt{ConvexArea}, \texttt{Perimeter}, \texttt{MajorAxisLength}, \texttt{MinorAxisLength} oraz \texttt{EquivDiameter}, zostały poddane transformacji logarytmicznej, co pozwoliło ograniczyć wpływ wartości ekstremalnych oraz zmniejszyć asymetrię rozkładów.

Następnie zastosowano procedurę usuwania obserwacji odstających opisaną w kolejnym podrozdziale. Standaryzację danych przeprowadzono dopiero na etapie końcowym, już po oczyszczeniu zbioru, poprzez centrowanie i skalowanie cech numerycznych.

Dzięki temu każda zmienna w końcowym zbiorze uzyskała średnią równą 0 oraz odchylenie standardowe równe 1, co jest zgodne z definicją standaryzacji Z-score.

Standaryzacja była szczególnie istotna z punktu widzenia wielomianowej regresji logistycznej oraz sieci neuronowej, ponieważ różnice skali zmiennych mogą wpływać na proces estymacji parametrów tych modeli.

\subsection{Braki danych}

Wybrany zbiór danych został otrzymany przez wykorzystanie wizji komputerowej zutomatycznie uzyskującej ze zdjęć komplet wszystkich cech. W otzymanych danych nie występują więc żadne braki. Dodatkowo poddano weryfikacji pod kątem występowania brakujących wartości. Potwierdzono kompletność cech we wszystkich pozycjach, co eliminuje konieczność stosowania imputacji.

\subsection{Obserwacje odstające}

Fizyczna natura obiektów biologicznych dopuszcza występowanie anomalii rozwojowych, takich jak ziarna uszkodzone mechanicznie, zdeformowane lub niepoprawnie wysegmentowane podczas przetwarzania obrazu. W celu identyfikacji obserwacji odstających zastosowano dwa niezależne podejścia: regułę 1.5 IQR oraz odległość Mahalanobisa.

W pierwszym etapie dla każdej zmiennej numerycznej wyznaczono granice:
\[
[Q1 - 1.5 \cdot IQR,\; Q3 + 1.5 \cdot IQR]
\]
gdzie $Q1$ i $Q3$ oznaczają kwartyle, a $IQR$ rozstęp międzykwartylowy. Obserwacje poza tym zakresem uznano za odstające w ujęciu jednowymiarowym.

W drugim etapie zastosowano analizę wielowymiarową opartą na odległości Mahalanobisa. Ze względu na silne korelacje między zmiennymi macierz kowariancji była bliska osobliwej, dlatego zastosowano jej regularizację poprzez dodanie małej wartości do przekątnej.

Za obserwacje odstające uznano rekordy wskazane przez co najmniej jedną metodę. Następnie zostały one usunięte ze zbioru danych.

%W tabeli \ref{tab:clean-stats} przedstawiono statystyki opisowe dla danych po oczyszczeniu.


% TODO: to wam działa? bo mi wywala i się nie kompiluje latex cały więc zakomentowałam
% \begin{table}[H]
% \centering
% \input{tabele_statystyki_opisowe_clean.tex}
% \end{table}


\subsection{Statystyki opisowe i podstawowa wizualizacja}

\input{tabele_statystyki_opisowe.tex}

\subsection{Transformacje Danych}

% (skalowanie, logarytmowanie)


Dane zostały następnie podzielone na zbiory testowe i treningowe z zachowaniem proporcji klas.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{proporcje_klas_original_train_test.pdf}
    \caption{Porównanie proporcji klas w całym zbiorze danych, zbiorze uczącym i zbiorze testowym.}
    \label{fig:proporcje-klas}
\end{figure}

Następnie zmienne numeryczne zostały przeskalowane oddzielnie dla zbioru uczącego i testowego, z wykorzystaniem parametrów ze zbioru uczącego. 


\input{tabele_statystyki_opisowe_przeskalowane.tex}


% ----------------------------------------------------


\chapter{Opis metod}

\noindent
W niniejszym rozdziale przedstawiono metody klasyfikacji wykorzystane do rozwiązania problemu przypisania ziaren fasoli do odpowiednich odmian. Analizowany problem ma charakter klasyfikacji wieloklasowej, ponieważ zmienna objaśniana może przyjmować więcej niż dwie możliwe wartości.

Niech zmienna objaśniana $Y$ oznacza klasę, czyli odmianę fasoli. W zbiorze \textit{Dry Bean Dataset} zmienna ta przyjmuje jedną z siedmiu wartości:
\[
Y \in \{\textit{SEKER}, \textit{BARBUNYA}, \textit{BOMBAY}, \textit{CALI}, \textit{DERMASON}, \textit{HOROZ}, \textit{SIRA}\}.
\]

Zmiennymi objaśniającymi są cechy geometryczne i morfologiczne opisujące pojedyncze ziarno fasoli. Dla pojedynczej obserwacji można je zapisać w postaci wektora:
\[
X = (X_1, X_2, \ldots, X_p),
\]
gdzie $p$ oznacza liczbę zmiennych objaśniających uwzględnionych w analizie.

Celem każdej z zastosowanych metod jest wyznaczenie reguły klasyfikacyjnej, która na podstawie wartości cech $X$ przypisuje obserwację do jednej z klas $Y$. Innymi słowy, dla każdego ziarna fasoli model podejmuje decyzję, do której odmiany jest ono najbardziej podobne ze względu na swoje cechy geometryczne.

W analizie wykorzystano zmienne opisujące między innymi rozmiar, kształt, wydłużenie oraz regularność ziarna. Do cech związanych z rozmiarem należą na przykład \textit{Area}, \textit{Perimeter}, \textit{ConvexArea}, \textit{MajorAxisLength} oraz \textit{MinorAxisLength}. Z kolei cechy takie jak \textit{Eccentricity}, \textit{Roundness}, \textit{Compactness}, \textit{AspectRatio} czy \textit{Solidity} opisują przede wszystkim kształt i proporcje ziarna.

Zastosowane metody różnią się sposobem wyznaczania granic decyzyjnych między klasami. Część z nich, na przykład wielomianowa regresja logistyczna oraz LDA, zakłada liniowy charakter granic między klasami. Inne, takie jak QDA, drzewa decyzyjne, Random Forest czy SVM, pozwalają na bardziej złożone granice decyzyjne. Dzięki temu możliwe jest porównanie zarówno prostszych, bardziej interpretowalnych metod statystycznych, jak i metod bardziej elastycznych.

W dalszej części rozdziału opisano kolejno zastosowane metody klasyfikacji oraz sposób podejmowania przez nie decyzji klasyfikacyjnych.

% ----------------------------------------------------

\section{Wielomianowa regresja logistyczna}

% Regresja logistyczna nie wymaga niezależności cech, ale silna współliniowość może utrudniać interpretację współczynników.


%"The multinomial logistic model assumes that data are case-specific; that is, each independent variable has a single value for each case. As with other types of regression, there is no need for the independent variables to be statistically independent from each other (unlike, for example, in a naive Bayes classifier); however, collinearity is assumed to be relatively low, as it becomes difficult to differentiate between the impact of several variables if this is not the case." wtf


Wielomianowa regresja logistyczna jest rozszerzeniem klasycznej regresji logistycznej na przypadek, w którym zmienna objaśniana przyjmuje więcej niż dwie kategorie. W analizowanym problemie metoda ta służy do oszacowania prawdopodobieństwa przynależności ziarna fasoli do każdej z rozważanych odmian.

Celem wielomianowej regresji logistycznej jest oszacowanie prawdopodobieństwa przynależności obserwacji do każdej z możliwych klas. Dla każdej obserwacji model wyznacza prawdopodobieństwa:
\[
P(Y = k \mid X),
\]
gdzie $k$ oznacza jedną z klas. Obserwacja zostaje następnie przypisana do tej klasy, dla której przewidywane prawdopodobieństwo jest największe.

W modelu wielomianowej regresji logistycznej jedna z klas jest wybierana jako klasa referencyjna. Dla pozostałych klas modeluje się logarytm ilorazu szans względem tej klasy. Jeżeli jako klasę referencyjną oznaczymy klasę $K$, to dla każdej klasy $k = 1, \ldots, K-1$ model ma postać:
\[
\log \left( \frac{P(Y = k \mid X)}{P(Y = K \mid X)} \right)
=
\beta_{0k} + \beta_{1k}X_1 + \beta_{2k}X_2 + \ldots + \beta_{pk}X_p.
\]

Oznacza to, że dla każdej klasy, z wyjątkiem klasy referencyjnej, estymowany jest osobny zestaw współczynników regresji. Współczynniki te opisują wpływ poszczególnych cech ziarna na względne prawdopodobieństwo przypisania obserwacji do danej klasy w porównaniu z klasą referencyjną.

Prawdopodobieństwa przynależności do poszczególnych klas można zapisać w postaci:
\[
P(Y = k \mid X)
=
\frac{\exp(\beta_{0k} + \beta_{1k}X_1 + \ldots + \beta_{pk}X_p)}
{1 + \sum_{j=1}^{K-1} \exp(\beta_{0j} + \beta_{1j}X_1 + \ldots + \beta_{pj}X_p)}
\]
dla $k = 1, \ldots, K-1$, natomiast dla klasy referencyjnej:
\[
P(Y = K \mid X)
=
\frac{1}
{1 + \sum_{j=1}^{K-1} \exp(\beta_{0j} + \beta_{1j}X_1 + \ldots + \beta_{pj}X_p)}.
\]

W kontekście analizowanego zbioru danych model ten pozwala określić, jak cechy geometryczne ziarna wpływają na prawdopodobieństwo zaklasyfikowania go do konkretnej odmiany fasoli. Przykładowo, cechy związane z rozmiarem ziarna, takie jak \textit{Area}, \textit{Perimeter} czy \textit{MajorAxisLength}, mogą mieć istotne znaczenie przy rozróżnianiu większych odmian fasoli od mniejszych. Z kolei cechy opisujące kształt, takie jak \textit{Roundness}, \textit{Compactness} czy \textit{Eccentricity}, mogą pomagać w odróżnianiu odmian bardziej okrągłych od bardziej wydłużonych.

Klasyfikacja nowej obserwacji odbywa się poprzez obliczenie przewidywanego prawdopodobieństwa dla każdej klasy i wybór klasy o największej wartości tego prawdopodobieństwa:
\[
\hat{Y} = \arg\max_{k} P(Y = k \mid X).
\]

Wielomianowa regresja logistyczna jest metodą parametryczną i stosunkowo łatwą do interpretacji. Jej zaletą jest możliwość uzyskania nie tylko przewidywanej klasy, ale również prawdopodobieństw przynależności do każdej z klas. Dzięki temu można analizować, z jaką pewnością model przypisuje dane ziarno do konkretnej odmiany.

Metoda ta zakłada jednak, że zależność pomiędzy zmiennymi objaśniającymi a logarytmem ilorazu szans jest liniowa. W praktyce oznacza to, że model może gorzej radzić sobie w sytuacjach, gdy granice między klasami mają silnie nieliniowy charakter. W przypadku zbioru \textit{Dry Bean} może to mieć znaczenie, ponieważ niektóre odmiany fasoli mogą być do siebie bardzo podobne pod względem wybranych cech morfologicznych.

W przeprowadzonej analizie wielomianowa regresja logistyczna została wykorzystana jako jedna z metod klasyfikacji statystycznej. Jej wyniki porównano z wynikami pozostałych metod na podstawie miar jakości klasyfikacji, takich jak macierz pomyłek, dokładność klasyfikacji oraz miary precyzji, czułości i wyniku $F_1$.

% ----------------------------------------------------

\section{Random Forest}

Las losowy (ang. \textit{Random Forest}) jest metodą zespołową opartą na wielu drzewach decyzyjnych. Każde drzewo tworzy własną regułę klasyfikacyjną, natomiast końcowa predykcja klasy jest wyznaczana przez głosowanie wszystkich drzew. Dla obserwacji $x$ i zbioru $B$ drzew klasyfikacyjnych można to zapisać jako:
\[
\hat{Y}(x)=\operatorname{mode}\left\{h_1(x),h_2(x),\ldots,h_B(x)\right\},
\]
gdzie $h_b(x)$ oznacza klasę przewidzianą przez $b$-te drzewo, a operator $\operatorname{mode}$ zwraca najczęściej występującą klasę.

Różnorodność drzew uzyskuje się przez zastosowanie dwóch mechanizmów losowych. Po pierwsze, każde drzewo jest budowane na osobnej próbie bootstrapowej, czyli próbie wylosowanej ze zwracaniem ze zbioru uczącego. Obserwacje, które nie znalazły się w próbie użytej do budowy danego drzewa, tworzą zbiór \textit{Out-of-Bag} (OOB) i mogą służyć do wewnętrznej oceny błędu modelu.

Po drugie, podczas wyznaczania każdego podziału drzewa rozważany jest jedynie losowy podzbiór dostępnych zmiennych objaśniających. Ogranicza to podobieństwo poszczególnych drzew i zmniejsza ryzyko, że wszystkie z nich będą budowane przede wszystkim na podstawie tych samych, najsilniejszych predyktorów. W klasyfikacji domyślna liczba cech rozważanych przy pojedynczym podziale wynosi zwykle około $\sqrt{p}$, gdzie $p$ jest liczbą zmiennych objaśniających.

Jakość potencjalnego podziału węzła jest oceniana na podstawie zmniejszenia jego nieczystości. W zastosowanej implementacji wykorzystywany jest indeks Giniego. Dla węzła $m$ ma on postać:
\[
G(m)=1-\sum_{k=1}^{K}\hat{p}_{mk}^{2},
\]
gdzie $\hat{p}_{mk}$ oznacza udział obserwacji klasy $k$ w węźle $m$. Wartość indeksu jest mała, gdy w węźle dominują obserwacje jednej klasy. Algorytm wybiera takie podziały, które prowadzą do możliwie największego zmniejszenia nieczystości węzłów potomnych.

W projekcie model zbudowano przy użyciu funkcji \texttt{randomForest} z pakietu o tej samej nazwie. Zastosowano $500$ drzew oraz włączono wyznaczanie istotności zmiennych. Pozostałe parametry, w tym liczba cech losowanych przy każdym podziale, pozostawiono na wartościach domyślnych dla zadania klasyfikacyjnego. Ustawienie ziarna generatora liczb losowych na wartość $42$ zapewnia powtarzalność wyników.

Oprócz przewidywanych klas model zwraca prawdopodobieństwa przynależności do poszczególnych odmian fasoli. Wyznaczono również dwie miary istotności predyktorów: \textit{Mean Decrease Accuracy}, opisującą spadek trafności po zaburzeniu wartości danej cechy, oraz \textit{Mean Decrease Gini}, określającą łączny udział cechy w zmniejszaniu nieczystości węzłów.

Zaletą lasu losowego jest możliwość modelowania zależności nieliniowych i interakcji między cechami bez konieczności wcześniejszego określania ich postaci. Metoda jest także mniej podatna na przeuczenie niż pojedyncze, głębokie drzewo decyzyjne. Jej ograniczeniem jest natomiast mniejsza interpretowalność w porównaniu z pojedynczym modelem parametrycznym, takim jak regresja logistyczna.

% ----------------------------------------------------

\section{Sieć neuronowa}

W analizie zastosowano jednokierunkową wielowarstwową sieć neuronową typu MLP (ang. \textit{Multilayer Perceptron}). Model składa się z warstwy wejściowej, jednej warstwy ukrytej oraz warstwy wyjściowej. Warstwa wejściowa zawiera $12$ wejść odpowiadających zmiennym objaśniającym wykorzystanym w projekcie, natomiast warstwa wyjściowa reprezentuje siedem możliwych klas fasoli.

Dla neuronu $j$ w warstwie ukrytej obliczana jest kombinacja liniowa wartości wejściowych, a następnie stosowana jest nieliniowa funkcja aktywacji:
\[
z_j=f\left(b_j+\sum_{i=1}^{p}w_{ij}x_i\right),
\]
gdzie $x_i$ oznacza wartość $i$-tej cechy, $w_{ij}$ jest wagą połączenia, $b_j$ wyrazem wolnym, a $f$ funkcją aktywacji. Dzięki zastosowaniu warstwy ukrytej sieć może odwzorowywać zależności nieliniowe, których nie uwzględnia wielomianowa regresja logistyczna.

W warstwie wyjściowej dla każdej klasy wyznaczany jest wynik, który następnie przekształca się za pomocą funkcji softmax:
\[
P(Y=k\mid X)=\frac{\exp(a_k)}{\sum_{j=1}^{K}\exp(a_j)},
\]
gdzie $a_k$ oznacza wartość wejściową neuronu odpowiadającego klasie $k$. Otrzymane wartości są nieujemne i sumują się do jedności, dlatego mogą być interpretowane jako przewidywane prawdopodobieństwa przynależności do poszczególnych klas. Ostatecznie wybierana jest klasa o największym prawdopodobieństwie:
\[
\hat{Y}=\arg\max_k P(Y=k\mid X).
\]

Sieć została zbudowana za pomocą funkcji \texttt{nnet} z pakietu \texttt{nnet}. Zastosowano jedną warstwę ukrytą złożoną z $8$ neuronów. Maksymalną liczbę iteracji uczenia ustawiono na $1000$, a limit liczby wag na $10000$. Przed uczeniem ustawiono ziarno generatora liczb losowych na wartość $42$, aby wyniki były powtarzalne.

W modelu zastosowano regularyzację wag określoną parametrem \texttt{decay}=0{,}001. Oznacza ona dodanie do funkcji celu kary za duże wartości wag. Mechanizm ten ogranicza nadmierne dopasowanie sieci do zbioru uczącego i sprzyja uzyskaniu modelu o lepszych zdolnościach generalizacyjnych.

Uczenie modelu polega na iteracyjnym doborze wag i wyrazów wolnych tak, aby zmniejszać błąd predykcji na zbiorze uczącym. Ponieważ zmienna objaśniana jest faktorem obejmującym więcej niż dwie klasy, funkcja \texttt{nnet} realizuje klasyfikację wieloklasową i zwraca zarówno przewidywaną klasę, jak i wektor wyników dla wszystkich klas.

Zaletą sieci neuronowej jest możliwość wykrywania złożonych zależności między cechami geometrycznymi ziarna. Jej ograniczeniami są mniejsza interpretowalność parametrów, zależność wyniku od konfiguracji modelu i punktu początkowego optymalizacji oraz możliwość przeuczenia przy zbyt rozbudowanej architekturze. Z tego powodu zastosowano niewielką warstwę ukrytą oraz regularyzację wag.

% ----------------------------------------------------

\section{Metoda hybrydowa}

Czwarty mechanizm decyzyjny stanowi model hybrydowy należący do metod zespołowych. Łączy on predykcje wielomianowej regresji logistycznej, lasu losowego oraz sieci neuronowej. Każdy z trzech modeli niezależnie przewiduje jedną z siedmiu klas, a wyniki są następnie agregowane za pomocą głosowania większościowego.

Dla obserwacji $x$ końcową decyzję można zapisać jako:
\[
\hat{Y}_{\mathrm{hyb}}(x)=\operatorname{mode}\left\{
\hat{Y}_{\mathrm{log}}(x),
\hat{Y}_{\mathrm{RF}}(x),
\hat{Y}_{\mathrm{NN}}(x)
\right\}.
\]
Jeżeli co najmniej dwa modele wskazują tę samą odmianę, zostaje ona przyjęta jako wynik końcowy. W przypadku trzech różnych predykcji zastosowana w kodzie funkcja wybiera pierwszą klasę po uporządkowaniu tabeli liczebności głosów, co zapewnia deterministyczne rozstrzygnięcie remisu.

Takie podejście pozwala połączyć odmienne właściwości klasyfikatorów. Regresja logistyczna dostarcza prostego modelu probabilistycznego, las losowy dobrze odwzorowuje nieliniowości i interakcje, a sieć neuronowa może rozpoznawać bardziej złożone wzorce. Jeżeli błędy modeli nie występują dla tych samych obserwacji, głosowanie może ograniczyć wpływ błędnej decyzji pojedynczego klasyfikatora.

W przedstawionym rozwiązaniu wszystkie trzy modele mają jednakową wagę, a głosowanie odbywa się na podstawie przewidywanych klas, nie zaś średniej przewidywanych prawdopodobieństw. Model hybrydowy nie jest więc osobno uczony; wykorzystuje wcześniej dopasowane klasyfikatory i stanowi dodatkową regułę decyzyjną. Jego jakość oceniono na tym samym zbiorze testowym i za pomocą tych samych mierników co jakość modeli składowych.

% ----------------------------------------------------

\section{Przegląd literatury dotyczącej metod}

Problematyka automatycznej klasyfikacji produktów rolniczych z wykorzystaniem cech pozyskiwanych z obrazów jest szeroko omawiana w literaturze. Zbiór \textit{Dry Bean Dataset} został opracowany przez Köklü i Özkana \cite{koklu2020}. Autorzy wyodrębnili z obrazów ziaren cechy geometryczne i morfologiczne, a następnie porównali skuteczność kilku klasyfikatorów. Uzyskane rezultaty potwierdziły, że informacje o rozmiarze i kształcie ziarna mogą być skutecznie wykorzystywane do rozpoznawania odmian fasoli.

Wielomianowa regresja logistyczna należy do klasycznych modeli probabilistycznych stosowanych w klasyfikacji wieloklasowej. Jej istotną zaletą jest możliwość interpretowania wpływu zmiennych poprzez ilorazy szans oraz uzyskiwania prawdopodobieństw przynależności do każdej klasy. Stanowi ona w badaniu punkt odniesienia dla bardziej elastycznych metod nieliniowych.

Metoda \textit{Random Forest} została zaproponowana przez Breimana \cite{breiman2001}. Łączy ona agregację bootstrapową z losowym doborem zmiennych w węzłach drzew, co ogranicza korelację pomiędzy klasyfikatorami składowymi i pozwala zmniejszyć wariancję predykcji. Właściwość ta jest szczególnie użyteczna w zbiorach zawierających liczne, wzajemnie skorelowane cechy.

Sieci neuronowe typu MLP są uniwersalnymi modelami aproksymacyjnymi zdolnymi do odwzorowywania złożonych zależności nieliniowych. W problemach klasyfikacji wieloklasowej często wykorzystuje się warstwę wyjściową z funkcją softmax, dzięki której wyniki można interpretować jako rozkład prawdopodobieństwa po klasach. Jednocześnie modele te wymagają doboru architektury, regularyzacji i parametrów procesu uczenia.

Połączenie klasyfikatorów za pomocą głosowania jest jednym z podstawowych sposobów budowania modeli zespołowych. Celem jest wykorzystanie różnorodności metod składowych: jeżeli popełniają one odmienne błędy, decyzja większościowa może być stabilniejsza od wyniku pojedynczego modelu. W niniejszym projekcie zastosowano głosowanie trzech modeli reprezentujących różne podejścia do wyznaczania granic decyzyjnych.


% ----------------------------------------------------

\chapter{Wyniki}

\section{Mierniki}

Do oceny jakości klasyfikacji wykorzystano macierz pomyłek, dokładność klasyfikacji (\textit{Accuracy}) oraz miary \textit{Precision}, \textit{Recall} i \textit{F1-score}. Ponieważ analizowane zadanie jest problemem klasyfikacji wieloklasowej, każda obserwacja może zostać przypisana do jednej z siedmiu odmian fasoli. Z tego względu macierz pomyłek ma wymiar $K \times K$, gdzie w rozpatrywanym przypadku $K=7$. Wiersze macierzy odpowiadają klasom rzeczywistym, natomiast kolumny klasom przewidzianym przez model. Elementy na głównej przekątnej oznaczają obserwacje sklasyfikowane poprawnie, a elementy poza przekątną przedstawiają pomyłki między poszczególnymi klasami.

Dokładność klasyfikacji jest jedną globalną miarą jakości całego modelu. W problemie wieloklasowym oblicza się ją jako udział wszystkich poprawnie sklasyfikowanych obserwacji w całkowitej liczbie obserwacji ze zbioru testowego:

\[
Accuracy = \frac{\sum_{k=1}^{K} n_{kk}}{N}
\]

gdzie $n_{kk}$ oznacza liczbę obserwacji klasy $k$ poprawnie przypisanych do tej samej klasy, a $N$ oznacza liczbę wszystkich obserwacji. W liczniku sumowane są zatem elementy znajdujące się na głównej przekątnej macierzy pomyłek. W przedstawionych wynikach nie wyznacza się oddzielnego \textit{Accuracy} dla każdej klasy. Jedna wartość tej miary podsumowuje skuteczność klasyfikatora dla wszystkich siedmiu klas jednocześnie.

Samo \textit{Accuracy} nie pokazuje jednak, dla których odmian model działa dobrze, a dla których popełnia najwięcej błędów. Dlatego dodatkowo obliczono \textit{Precision}, \textit{Recall} oraz \textit{F1-score} osobno dla każdej klasy. W przypadku klasyfikacji wieloklasowej miary te wyznaczono w schemacie jeden-przeciwko-reszcie (\textit{one-vs-rest}). Dla rozpatrywanej klasy $k$ jest ona traktowana jako klasa pozytywna, natomiast wszystkie pozostałe klasy łącznie jako klasa negatywna.

Dla każdej klasy $k$ zdefiniowano:

\[
Precision_k=\frac{TP_k}{TP_k+FP_k},
\]

\[
Recall_k=\frac{TP_k}{TP_k+FN_k},
\]

\[
F1_k=2\cdot\frac{\mathrm{Precision}_k\cdot\mathrm{Recall}_k}
{\mathrm{Precision}_k+\mathrm{Recall}_k}.
\]

Wielkość $TP_k$ oznacza liczbę obserwacji klasy $k$ poprawnie rozpoznanych przez model. $FP_k$ oznacza liczbę obserwacji należących do innych klas, które błędnie przypisano do klasy $k$, natomiast $FN_k$ oznacza liczbę obserwacji klasy $k$, które błędnie przypisano do jednej z pozostałych klas. \textit{Precision} określa, jaka część obserwacji przewidzianych jako dana odmiana rzeczywiście do niej należy. \textit{Recall} informuje, jaka część wszystkich rzeczywistych obserwacji danej odmiany została poprawnie rozpoznana. \textit{F1-score} jest średnią harmoniczną tych dwóch miar i uwzględnia jednocześnie oba rodzaje błędów.

Wyniki każdego klasyfikatora przedstawiono zatem na dwóch poziomach. Globalne \textit{Accuracy} służy do ogólnego porównania modeli, natomiast wartości \textit{Precision}, \textit{Recall} i \textit{F1-score} pozwalają ocenić jakość klasyfikacji osobno dla każdej z siedmiu odmian fasoli.

% ----------------------------------------------------

\section{Sposób walidacji}

Do oceny zdolności modeli do klasyfikowania nowych obserwacji zastosowano walidację typu \textit{hold-out}. Polega ona na jednorazowym podziale zbioru danych na rozłączne zbiory: uczący oraz testowy. W przeprowadzonej analizie $70\%$ obserwacji przeznaczono do uczenia modeli, a pozostałe $30\%$ do ich końcowej oceny.

Podział wykonano w sposób stratyfikowany, czyli oddzielnie w obrębie każdej klasy. Dzięki temu w zbiorze uczącym i testowym zachowano proporcje siedmiu odmian fasoli zbliżone do proporcji występujących w całym oczyszczonym zbiorze danych. Jest to szczególnie istotne w analizowanym problemie, ponieważ liczebności poszczególnych klas nie są jednakowe. Dla zapewnienia powtarzalności losowania ustawiono ziarno generatora liczb pseudolosowych na wartość 42.

Zbiór uczący wykorzystano do estymacji parametrów wielomianowej regresji logistycznej, lasu losowego oraz sieci neuronowej. Zbiór testowy nie uczestniczył w procesie uczenia i służył wyłącznie do obliczenia końcowych wartości mierników jakości. Metodę hybrydową oceniono na tym samym zbiorze testowym, łącząc predykcje trzech wcześniej wytrenowanych klasyfikatorów.

Aby uniknąć przenikania informacji ze zbioru testowego do procesu uczenia, parametry standaryzacji, czyli średnie i odchylenia standardowe zmiennych, wyznaczono wyłącznie na podstawie zbioru uczącego. Następnie te same parametry zastosowano do przeskalowania zbioru testowego. Wszystkie modele oceniono na identycznych obserwacjach testowych i przy użyciu tego samego zestawu mierników: macierzy pomyłek, \textit{Accuracy}, \textit{Precision}, \textit{Recall} oraz \textit{F1-score}. Pozwala to na bezpośrednie i porównywalne zestawienie wyników poszczególnych metod.

% ----------------------------------------------------

\section{Wielomianowa regresja logistyczna}

\subsection*{Metryki}

\input{wyniki_regresja_logistyczna.tex}

% ----------------------------------------------------

\section{Sieć neuronowa}

W analizie zastosowano jednokierunkową, wielowarstwową sieć neuronową typu MLP, czyli \textit{Multilayer Perceptron}. 

\subsection*{Architektura}

Zastosowana sieć składała się z trzech głównych części: warstwy wejściowej, jednej warstwy ukrytej oraz warstwy wyjściowej. 



\begin{figure}[H]
\centering
\begin{tikzpicture}[
    x=1cm, y=1cm,
    >=Latex,
    neuron/.style={circle, draw, minimum size=0.9cm, inner sep=0pt},
    dots/.style={draw=none, fill=none},
    every node/.style={font=\small}
]

% ------------------------
% INPUT LAYER
% ------------------------
\node[neuron] (I1) at (0, 6) {$X_1$};
\node[neuron] (I2) at (0, 4.3) {$X_2$};
\node[neuron] (I3) at (0, 2.6) {$X_3$};
\node[dots]   (Id) at (0, 1.4) {$\vdots$};
\node[neuron] (I12) at (0, 0) {$X_{12}$};

% ------------------------
% HIDDEN LAYER
% ------------------------
\node[neuron] (H1) at (4, 6) {$h_1$};
\node[neuron] (H2) at (4, 4.3) {$h_2$};
\node[neuron] (H3) at (4, 2.6) {$h_3$};
\node[dots]   (Hd) at (4, 1.4) {$\vdots$};
\node[neuron] (H8) at (4, 0) {$h_8$};

% ------------------------
% OUTPUT LAYER
% ------------------------
\node[neuron] (O1) at (8, 6.0) {$y_1$};
\node[neuron] (O2) at (8, 5.0) {$y_2$};
\node[neuron] (O3) at (8, 4.0) {$y_3$};
\node[neuron] (O4) at (8, 3.0) {$y_4$};
\node[neuron] (O5) at (8, 2.0) {$y_5$};
\node[neuron] (O6) at (8, 1.0) {$y_6$};
\node[neuron] (O7) at (8, 0.0) {$y_7$};

% ------------------------
% CONNECTIONS INPUT -> HIDDEN
% ------------------------
\foreach \i in {1,2,3,12}
{
    \foreach \h in {1,2,3,8}
    {
        \draw[->, thin] (I\i) -- (H\h);
    }
}

% connections from dots (visual suggestion only)
%\draw[->, thin] (Id) -- (H1);
%\draw[->, thin] (Id) -- (H8);
%\draw[->, thin] (I1) -- (Hd);
%\draw[->, thin] (I16) -- (Hd);

% ------------------------
% CONNECTIONS HIDDEN -> OUTPUT
% ------------------------
\foreach \h in {1,2,3,8}
{
    \foreach \o in {1,2,3,4,5,6,7}
    {
        \draw[->, thin] (H\h) -- (O\o);
    }
}

% connections from hidden dots
%\draw[->, thin] (Hd) -- (O1);
%\draw[->, thin] (Hd) -- (O7);

% ------------------------
% LAYER LABELS
% ------------------------
\node[align=center, font=\bfseries] at (0, 6.9) {Warstwa\\wejściowa};
\node[align=center, font=\bfseries] at (4, 6.9) {Warstwa\\ukryta};
\node[align=center, font=\bfseries] at (8, 6.9) {Warstwa\\wyjściowa};

% ------------------------
% EXTRA LABELS
% ------------------------
\node[align=center] at (0, -1.0) {12 cech};
\node[align=center] at (4, -1.0) {8 neuronów};
\node[align=center] at (8, -1.0) {7 klas};

% ------------------------
% OUTPUT CLASS NAMES
% ------------------------
\node[anchor=west] at (8.7, 6.0) {\textit{SEKER}};
\node[anchor=west] at (8.7, 5.0) {\textit{BARBUNYA}};
\node[anchor=west] at (8.7, 4.0) {\textit{BOMBAY}};
\node[anchor=west] at (8.7, 3.0) {\textit{CALI}};
\node[anchor=west] at (8.7, 2.0) {\textit{DERMASON}};
\node[anchor=west] at (8.7, 1.0) {\textit{HOROZ}};
\node[anchor=west] at (8.7, 0.0) {\textit{SIRA}};

\end{tikzpicture}
\caption{Schemat architektury zastosowanej sieci neuronowej.}
\label{fig:siec-neuronowa}
\end{figure}


Zastosowana sieć składała się z trzech głównych części: warstwy wejściowej, jednej warstwy ukrytej oraz warstwy wyjściowej. Warstwa wejściowa miała tyle wejść, ile zmiennych objaśniających. 

Pomiędzy warstwą wejściową a warstwą wyjściową zastosowano jedną warstwę ukrytą złożoną z 8 neuronów. Liczbę neuronów w tej warstwie określa parametr \texttt{size}. W przeprowadzonej analizie przyjęto:
\[
\texttt{size} = 8.
\]
Warstwa ukryta umożliwia modelowi uchwycenie nieliniowych zależności między cechami wejściowymi a klasą fasoli. Dzięki temu sieć neuronowa może modelować bardziej złożone granice decyzyjne niż wielomianowa regresja logistyczna.

%funkcja aktywacji

W warstwie wyjściowej zastosowano tyle neuronów, ile było klas zmiennej objaśnianej, czyli 7. 


Ponieważ analizowany problem jest problemem klasyfikacji wieloklasowej, na warstwie wyjściowej zastosowano funkcję \textit{softmax}. Ostateczna klasyfikacja odbywa się poprzez wybór klasy o największym przewidywanym prawdopodobieństwie.


W modelu zastosowano również regularyzację wag określoną parametrem \texttt{decay}. W analizie przyjęto:
\[
\texttt{decay} = 0.001.
\]
Regularyzacja ogranicza nadmierny wzrost wartości wag, co zmniejsza ryzyko przeuczenia modelu. Oznacza to, że sieć nie tylko dopasowuje się do danych uczących, ale jest również zachęcana do zachowania prostszej struktury wag.



Proces uczenia sieci polegał na iteracyjnym doborze wag połączeń między neuronami w taki sposób, aby minimalizować błąd klasyfikacji na zbiorze uczącym. Maksymalną liczbę iteracji ustalono na:
\[
\texttt{maxit} = 1000.
\]
Dodatkowo parametr \texttt{MaxNWts} określał maksymalną dopuszczalną liczbę wag w modelu. W analizie ustawiono:
\[
\texttt{MaxNWts} = 10000,
\]
aby uniknąć ograniczeń technicznych związanych z liczbą parametrów sieci.

\subsection*{Metryki}

\input{wyniki_siec_neuronowa.tex}

% ----------------------------------------------------

\section{Las losowy (Random Forest)}

Las losowy (ang. \textit{Random Forest}) stanowi zaawansowaną metodę uczenia zespołowego (ang. \textit{ensemble learning}), łączącą podejście nieparametryczne z dużą elastycznością dopasowania. Algorytm ten bazuje na konstrukcji dużej liczby niezależnych od siebie algorytmów słabych — w tym przypadku pojedynczych drzew decyzyjnych — a następnie agregacji ich predykcji poprzez głosowanie większościowe.

Kluczem do skuteczności lasu losowego jest redukcja wariancji pojedynczych drzew bez jednoczesnego zwiększania ich obciążenia. Cel ten osiągany jest poprzez wprowadzenie dwupoziomowej losowości:
\begin{enumerate}
    \item \textbf{Agregacja bootstrapowa (Bagging):} Każde drzewo w zespole trenowane jest na osobnym podzbiorze obserwacji, losowanym ze zwracaniem z oryginalnego zestawu danych treningowych. Obserwacje, które nie zostaną wybrane do próby uczącej danego drzewa, tworzą zbiór \textit{Out-of-Bag} (OOB) i są wykorzystywane do wewnętrznej, bezstronnej oceny błędu generalizacji modelu.
    \item \textbf{Losowe podprzestrzenie cech (Random Subspace Method):} Podczas podziału węzła w procesie wzrostu drzewa, algorytm nie poszukuje optymalnego podziału spośród wszystkich dostępnych predyktorów. Zamiast tego wybiera losowy podzbiór cech o określonej liczności (standardowo dla klasyfikacji przyjmuje się pierwiastek z łącznej liczby cech). Procedura ta drastycznie zmniejsza skorelowanie poszczególnych drzew w lesie, dzięki czemu błędy pojedynczych klasyfikatorów wzajemnie się znoszą.
\end{enumerate}

Dla wektora cech geometrycznych ziarna, ostateczna decyzja klasyfikacyjna modelu podejmowana jest w oparciu o głosowanie większościowe wszystkich wygenerowanych drzew decyzyjnych. Algorytm pozwala także na precyzyjną ocenę istotności predyktorów w procesie różnicowania odmian fasoli za pomocą miar spadku dokładności klasyfikacji (MDA) oraz spadku nieczystości węzłów mierzony współczynnikiem Giniego (MDG).

\subsection*{Metryki}

Ewaluację modelu lasu losowego przeprowadzono na zbiorze testowym przy zachowaniu procedur preprocessingu (skalowanie predyktorów na podstawie parametrów wyznaczonych wyłącznie ze zbioru treningowego). Szczegółowe zestawienie macierzy pomyłek, ogólnej dokładności, miar jakości klasyfikacji dla poszczególnych odmian oraz istotności zmiennych przedstawiono w tabelach poniżej.

\input{wyniki_random_forest.tex}

% ----------------------------------------------------

\section{Metoda hybrydowa}

W celu sprawdzenia, czy połączenie kilku klasyfikatorów pozwoli uzyskać lepsze wyniki klasyfikacji, zastosowano metodę hybrydową. Końcowa decyzja klasyfikacyjna była wyznaczana na podstawie głosowania większościowego trzech modeli: wielomianowej regresji logistycznej, lasu losowego oraz sieci neuronowej.

Poniżej przedstawiono wyniki uzyskane przez model hybrydowy na zbiorze testowym. Zaprezentowano macierz pomyłek, dokładność klasyfikacji oraz wartości miar Precision, Recall i F1-score dla poszczególnych klas fasoli.

\input{wyniki_hybrydowe.tex}

Uzyskane wyniki pozwalają ocenić skuteczność podejścia zespołowego oraz porównać je z rezultatami osiągniętymi przez pojedyncze modele przedstawione we wcześniejszych podrozdziałach.

% ----------------------------------------------------

\chapter{Przykład użycia modeli na stworzonych sztucznie obserwacjach}

Oprócz oceny modeli na wydzielonym zbiorze testowym przeprowadzono również przykład klasyfikacji sztucznie wygenerowanych obserwacji. Celem tej części nie jest dodatkowa walidacja jakości modeli, ponieważ wygenerowane rekordy nie stanowią niezależnych danych pochodzących z rzeczywistych pomiarów. Przykład ma jedynie pokazać sposób wykorzystania wytrenowanych klasyfikatorów do przewidywania klasy nowych obserwacji.

Sztuczne obserwacje wygenerowano na podstawie zbioru uczącego po przeprowadzeniu standaryzacji. Oznacza to, że nowe rekordy tworzono bezpośrednio w tej samej przestrzeni cech, w której uczone były modele. Nie było więc konieczne ponowne skalowanie wygenerowanych danych.

Dla każdej z siedmiu odmian fasoli utworzono po dwie sztuczne obserwacje. Najpierw dla każdej klasy i każdej zmiennej objaśniającej wyznaczono średnią oraz odchylenie standardowe na podstawie obserwacji należących do danej klasy w zbiorze uczącym.

Następnie wartości sztucznych cech losowano z rozkładu normalnego:

\[
X_j^{*} \sim N\left(\overline{x}_{jk},
\left(0{,}25s_{jk}\right)^2\right),
\]

gdzie $\overline{x}_{jk}$ oznacza średnią cechy $j$ dla klasy $k$, natomiast $s_{jk}$ oznacza jej odchylenie standardowe. Zastosowanie jednej czwartej odchylenia standardowego powoduje, że generowane punkty znajdują się stosunkowo blisko typowych obserwacji danej klasy.

Aby ograniczyć możliwość powstawania skrajnych wartości, każdy wylosowany wynik został dodatkowo ograniczony do minimalnej i maksymalnej wartości danej cechy obserwowanej w zbiorze uczącym. W celu zapewnienia powtarzalności wyników przed generowaniem danych ustawiono ziarno generatora liczb pseudolosowych za pomocą polecenia \texttt{set.seed(123)}.

Przyjęte podejście generuje każdą cechę osobno i nie odtwarza pełnej struktury korelacji pomiędzy zmiennymi. Z tego względu sztuczne rekordy należy traktować jako przykładowe punkty znajdujące się w pobliżu środka danej klasy, a nie jako dokładną symulację rzeczywistych ziaren fasoli.

Dla każdej sztucznej obserwacji wykonano predykcję za pomocą wielomianowej regresji logistycznej, lasu losowego oraz sieci neuronowej. Następnie na podstawie wyników tych trzech modeli wyznaczono predykcję metody hybrydowej, wykorzystując głosowanie większościowe.

\input{wyniki_sztuczne_dane.tex}

Kolumna \textit{Klasa bazowa} wskazuje odmianę fasoli, na podstawie której obliczono parametry użyte do wygenerowania danej obserwacji. Nie jest ona rzeczywistą etykietą pochodzącą z pomiaru, lecz oczekiwanym punktem odniesienia. Zgodność przewidywanej klasy z klasą bazową oznacza, że model rozpoznał wygenerowany układ cech jako typowy dla danej odmiany.

W większości przypadków wszystkie klasyfikatory wskazały klasę zgodną z klasą bazową. Pełną zgodność wszystkich modeli uzyskano między innymi dla obu obserwacji klasy \textit{BOMBAY} oraz obu obserwacji klasy \textit{HOROZ}. Świadczy to o tym, że sztuczne punkty wygenerowane dla tych klas znajdowały się w obszarach jednoznacznie rozpoznawanych przez zastosowane modele.

Pojedyncze różnice wystąpiły przede wszystkim w przypadku wielomianowej regresji logistycznej oraz sieci neuronowej. Dla drugiej obserwacji bazowej \textit{BARBUNYA} regresja logistyczna wskazała klasę \textit{SEKER}, podczas gdy pozostałe modele wybrały \textit{BARBUNYA}. Podobna sytuacja wystąpiła dla jednej obserwacji klasy \textit{DERMASON}, którą regresja logistyczna przypisała do klasy \textit{SEKER}. Dla jednej obserwacji bazowej \textit{CALI} sieć neuronowa wskazała klasę \textit{SIRA}, natomiast dla jednej obserwacji bazowej \textit{SEKER} przypisała klasę \textit{DERMASON}.

Największe różnice w predykcjach regresji logistycznej wystąpiły dla obserwacji bazowych klasy \textit{SIRA}. Zostały one przypisane odpowiednio do klas \textit{CALI} oraz \textit{HOROZ}, podczas gdy las losowy i sieć neuronowa poprawnie wskazały klasę \textit{SIRA}. Może to wynikać z bardziej liniowego charakteru granic decyzyjnych regresji logistycznej oraz podobieństwa części cech morfologicznych pomiędzy tymi odmianami.

Metoda hybrydowa przypisała wszystkie sztuczne obserwacje do ich klas bazowych. Wynikało to z faktu, że w każdym przypadku co najmniej dwa spośród trzech modeli składowych wskazywały klasę zgodną z klasą bazową. W tym przykładzie głosowanie większościowe pozwoliło więc skorygować pojedyncze błędne predykcje poszczególnych klasyfikatorów.

Uzyskanych wyników nie należy jednak interpretować jako dodatkowej miary skuteczności modeli. Sztuczne obserwacje powstały na podstawie statystyk tego samego zbioru uczącego, na którym modele były trenowane, dlatego ich klasyfikacja jest łatwiejsza niż klasyfikacja całkowicie niezależnych danych. Wiarygodna ocena zdolności generalizacji modeli została przeprowadzona wcześniej na wydzielonym zbiorze testowym.



\begin{thebibliography}{9}

%TODO

\bibitem{koklu2020}
M. Köklü, I. A. Özkan,
\textit{Multiclass classification of dry beans using computer vision and machine learning techniques},
Computers and Electronics in Agriculture, 174, 105507, 2020.
\url{https://doi.org/10.1016/j.compag.2020.105507}.

\bibitem{breiman2001}
L. Breiman,
\textit{Random Forests},
Machine Learning, 45, s. 5--32, 2001.
\url{https://doi.org/10.1023/A:1010933404324}.

\end{thebibliography}

\end{document}
